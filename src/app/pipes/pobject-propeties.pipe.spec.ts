import { PObjectPropetiesPipe } from './pobject-propeties.pipe';

describe('PObjectPropetiesPipe', () => {
  it('create an instance', () => {
    const pipe = new PObjectPropetiesPipe();
    expect(pipe).toBeTruthy();
  });
});
